{
  "deny-web-fetch": {
    "description": "Blocks network downloaders / web-fetch CLIs (curl, wget, aria2c, axel). Requires bash-deny and jq on PATH; no-ops if either is missing.",
    "hook": "tool_call",
    "filter": { "toolName": "bash" },
    "when": "command -v bash-deny >/dev/null 2>&1 && command -v jq >/dev/null 2>&1",
    "shell": "CMD=$(printf '%s' \"$PI_TOOL_INPUT\" | jq -r '.command // empty'); [ -z \"$CMD\" ] || bash-deny -r 'curl;wget;aria2c;axel' -i \"$CMD\" -q"
  }
}
